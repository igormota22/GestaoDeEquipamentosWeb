@{
    Layout = "_Layout";
    ViewBag.Titulo = "Exclusão de Chamado";
}



@model ExcluirChamadoViewModel;

<form class="formulario" asp-action="Excluir" asp-route-id="@Model.Id" method="post">

    <p class="formulario-alerta">Deseja mesmo excluir este chamado?</p>

    <div class="formulario-campo">
        <label asp-for="Titulo">Titulo</label>
        <br>
        <input asp-for="Titulo" type="text" value="@Model.Titulo" disabled>
    </div>

    <div class="formulario-campo">
        <label asp-for="Descricao">Descrição</label>
        <br>
        <input asp-for="Descricao" type="text" value="@Model.Descricao" disabled>
    </div>

    <div class="formulario-campo">
        <label asp-for="Equipamento">Equipamento</label>
        <input asp-for="Equipamento" type="text" value="@Model.Equipamento" disabled>
    </div>

    <div class="formulario-acoes">
        <a asp-action="Listar">Voltar</a>
        <button type="submit">Confirmar</button>
    </div>

</form>