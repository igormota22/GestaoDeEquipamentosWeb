@using GestaoDeEquipamentosWeb.ConsoleApp.Models
@{
    Layout = "_Layout";
    ViewBag.Titulo = "Exclusão de equipamento";


}


@model ExcluirEquipamentoViewModel;

<form class="formulario" asp-action="Excluir" asp-route-id="@Model.Id" method="post">

    <p class="formulario-alerta">Deseja mesmo excluir este equipamento?</p>

    <div class="formulario-campo">
        <label asp-for="Nome">Nome</label>
        <input asp-for="Nome" type="text" value="@Model.Nome" disabled>
    </div>

    <div class="formulario-campo">
        <label asp-for="PrecoAquisicao">Preço de Aquisição</label>
        <input asp-for="PrecoAquisicao" type="text" value="@Model.PrecoAquisicao" disabled>
    </div>

    <div class="formulario-campo">
        <label asp-for="DataFabricacao">Data De Fabricação</label>
        <input asp-for="DataFabricacao" type="text" value="@Model.DataFabricacao.ToShortDateString()" disabled>
    </div>

    <div class="formulario-campo">
        <label asp-for="Fabricante">Fabricante</label>
        <input asp-for="Fabricante" type="text" value="@Model.Fabricante" disabled>
    </div>

    <div class="formulario-acoes">
        <a asp-action="Listar">Voltar</a>
        <button type="submit">Confirmar</button>
    </div>

</form>