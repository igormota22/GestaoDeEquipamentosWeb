﻿@using GestaoDeEquipamentosWeb.ConsoleApp.Models
@{
    Layout = "_Layout";
    ViewBag.Titulo = "Edição de fabricante";

}

@model EditarFabricanteViewModel;

<form class="formulario" asp-action="Editar" asp-route-id="@Model.Id" method="post">
    <div class="formulario-campo">
        <label asp-for="Nome">Nome</label>
        <input asp-for="Nome" type="text" value="@Model.Nome">
        <span asp-validation-for="Nome"></span>
    </div>

    <div class="formulario-campo">
        <label asp-for="Email">Email</label>
        <input asp-for="Email" type="email" value="@Model.Email">
        <span asp-validation-for="Email"></span>
    </div>

    <div class="formulario-campo">
        <label asp-for="Telefone">Telefone</label>
        <input asp-for="Telefone" type="text" value="@Model.Telefone">
        <span asp-validation-for="Telefone"></span>
    </div>
    <div class="formulario-acoes">
        <a asp-action="Listar">Voltar</a>
        <button type="submit">Confirmar</button>
    </div>

</form>