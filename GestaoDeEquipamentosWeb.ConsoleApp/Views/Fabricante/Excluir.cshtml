﻿@{
    Layout = "_Layout";
    ViewBag.Titulo = "Exclusão de fabricante";

}

@using GestaoDeEquipamentosWeb.ConsoleApp.Models

@model ExcluirFabricanteViewModel;

<form class="formulario" asp-action="Excluir" asp-route-id="@Model.Id" method="post">

    <p class="formulario-alerta">Deseja mesmo excluir este fabricante?</p>

    <div class="formulario-campo">
        <label asp-for="Nome">Nome</label>
        <input asp-for="Nome" type="text" value="@Model.Nome" disabled>
    </div>

    <div class="formulario-campo">
        <label asp-for="Email">Email</label>
        <input asp-for="Email" type="email" value="@Model.Email" disabled>
    </div>

    <div class="formulario-campo">
        <label asp-for="Telefone">Telefone</label>
        <input asp-for="Telefone" type="text" value="@Model.Telefone" disabled>
    </div>

    <div class="formulario-acoes">
        <a asp-action="Listar">Voltar</a>
        <button type="submit">Confirmar</button>
    </div>

   
</form>